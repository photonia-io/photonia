<% content_for(:page_title, 'Albums' + (@pagy.page == 1 ? '' : " - Page #{@pagy.page}")) %>
<h1>Albums</h1>
<hr>
<div>
  <% @albums.each do |album| %>
    <div>
      <a href="<%= album_path(album) %>">
        <% if cover_photo_url = album&.photos&.first&.image_url(:medium_intelligent).presence || album&.photos&.first&.image_url(:medium_square) %>
          <img src="<%= cover_photo_url %>" />
        <% end %>
        <%= album.title %>
      </a> (<%= album.albums_photos.size %> <%= album.albums_photos.size == 1 ? 'photo' : 'photos' %>)
    </div>
  <% end %>
</div>
<hr>
<%== pagy_bulma_nav(@pagy) %>
