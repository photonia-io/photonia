---
description: Run RuboCop linter and fix issues
---

Run RuboCop to check code quality:

```bash
bundle exec rubocop
```

If there are any offenses, analyze them and offer to fix them automatically with:

```bash
bundle exec rubocop -a
```

For more complex offenses that require manual intervention, offer to help fix them.
